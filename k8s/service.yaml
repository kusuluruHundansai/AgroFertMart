apiVersion: v1
kind: Service
metadata:
  name: agrofertmart-service

spec:
  type: NodePort

  selector:
    app: agrofertmart

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007